import { useStore } from './hooks';
import { setCategories } from './storeActions';
import StoreProvider from './StoreProvider';

export {
    StoreProvider,
    useStore,
    // actions
    setCategories,
};
